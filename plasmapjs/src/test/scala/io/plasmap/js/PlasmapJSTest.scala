package io.plasmap.js

import io.plasmap.querymodel.{PMCityFromCoordinates, PMCityFromName}
import utest._

object PlasmapJSTest extends TestSuite {

 // val jb = PlasmapJS
  def tests = TestSuite {
//    "can build city from name"-{
//      assert(jb.city("Frankfurt") == PMCityFromName("Frankfurt"))
//    }
//    "can build city from lon lat"-{
//      assert(jb.city(12.0, 1.0) == PMCityFromCoordinates(1.0f, 12.0f))
//    }
//    "can compose various transformers"-{
//      val t1 = jb.city("DD")
//      val t2 = jb.city(23.0, 12.0)
//      val composed = jb.compose(t1, t2)
//      assert(composed == Compose(Seq(CityFromName("DD"), CityFromCoordinates(12.0f, 23.0f))))
//    }
  }
}
